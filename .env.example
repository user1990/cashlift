# Copy to .env.local for local development.
# Demo mode is the default outside production when CASHLIFT_APP_MODE is unset.

# CASHLIFT_APP_MODE=demo
# CASHLIFT_APP_MODE=production

NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Optional: override the canonical site URL (defaults to production deployment URL).
# NEXT_PUBLIC_SITE_URL=http://localhost:3000
