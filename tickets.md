# Tickets: Dashboard URL date range

Persist the CashLift overview date range in the URL without changing the existing data-fetching architecture.

Work the **frontier**: any ticket whose blockers are all done.

## Dashboard: Persist overview date range in the URL

**What to build:** A selected overview date range can be refreshed, shared, and revisited with Back and Forward while the dashboard continues to request and display that range.

**Blocked by:** None — can start immediately.

- [x] Keep the default forecast range out of the URL and preserve the existing date-range validation.
- [x] Verify URL restoration and the existing dashboard query behavior.
